<Window x:Class="TODO_list.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TODO_list"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBox" x:Key="TextBoxStyle">
            <Setter Property="Margin" Value="5, 5, 5, 5"/>
        </Style>
        <Style TargetType="Label" x:Key="LabelStyle">
            <Setter Property="VerticalAlignment" Value="Bottom"/>
        </Style>
        <Style TargetType="Button" x:Key="ButtonStyle">
            <Setter Property="Margin" Value="5, 5, 5, 5"/>
        </Style>
        <Style TargetType="DatePicker" x:Key="DatePickerStyle">
            <Setter Property="Margin" Value="5, 5, 5, 5"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DataGrid ItemsSource="{Binding Quests}" SelectedItem="{Binding SelectedQuest}"/>
        <StackPanel Grid.Column="1">
            <Label Style="{StaticResource LabelStyle}">Task text</Label>
            <TextBox Style="{StaticResource TextBoxStyle}" Text="{Binding InputtedText, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Style="{StaticResource LabelStyle}">Deadline</Label>
            <DatePicker Style="{StaticResource DatePickerStyle}" SelectedDate="{Binding InputtedDeadline, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Style="{StaticResource LabelStyle}">Importance</Label>
            <TextBox Style="{StaticResource TextBoxStyle}" Text="{Binding InputtedImportance, UpdateSourceTrigger=PropertyChanged}" PreviewTextInput="IntegerCheck"/>
            <Grid Grid.Row="6" Height="50">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Style="{StaticResource ButtonStyle}" Command="{Binding AddCommand}" Grid.Column="0">Add</Button>
                <Button Style="{StaticResource ButtonStyle}" Command="{Binding DeleteCommand}" Grid.Column="1">Delete</Button>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
